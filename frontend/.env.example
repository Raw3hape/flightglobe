# Публичный WebSocket URL backend-сервера для production.
# В dev — не задавай, используется ws://localhost:8080/ws.
# Пример после деплоя backend:
#   VITE_WS_URL=wss://flightglobe-backend.fly.dev/ws
VITE_WS_URL=
