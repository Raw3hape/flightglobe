// Bloom/glow удалён намеренно — чистый профессиональный data-viz стиль,
// без неона. Оставляем пустой компонент, чтобы не ломать импорты,
// но не рендерим в App.tsx.
export function Effects() {
  return null;
}
